[project]
name = "comic-checker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "curl-cffi>=0.15.0",
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "pyyaml>=6.0.3",
    "rich>=14.0.0",
]
